import { ErreurSaisieDirective } from './erreur-saisie.directive';

describe('ErreurSaisieDirective', () => {
  it('should create an instance', () => {
    const directive = new ErreurSaisieDirective();
    expect(directive).toBeTruthy();
  });
});
