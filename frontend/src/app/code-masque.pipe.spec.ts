import { CodeMasquePipe } from './code-masque.pipe';

describe('CodeMasquePipe', () => {
  it('create an instance', () => {
    const pipe = new CodeMasquePipe();
    expect(pipe).toBeTruthy();
  });
});
